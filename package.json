{
  "name": "opensucker",
  "private": true,
  "version": "0.2.0",
  "description": "OpenSucker — Smart Trading Matrix (frontend_user + backend + MiniFish + InfoFish orchestrator)",
  "scripts": {
    "setup": "npm-run-all -s setup:backend setup:minifish setup:infofish setup:frontend_user",
    "setup:backend": "cd backend && uv sync",
    "setup:minifish": "cd MiniFish/backend && uv sync",
    "setup:infofish": "cd InfoFish/backend && uv sync",
    "setup:frontend_user": "cd frontend_user && npm install",

    "dev": "concurrently -n backend,minifish,infofish,frontend_user -c blue,magenta,yellow,green --kill-others-on-fail \"npm:dev:backend\" \"npm:dev:minifish\" \"npm:dev:infofish\" \"npm:dev:frontend_user\"",
    "dev:backend": "cd backend && uv run python -u agent.py",
    "dev:minifish": "cd MiniFish/backend && uv run python -u run.py",
    "dev:infofish": "cd InfoFish/backend && uv run uvicorn app.main:app --host 127.0.0.1 --port 47821",
    "dev:frontend_user": "cd frontend_user && npm run dev -- -p 3000",

    "start": "npm run dev",

    "build:frontend_user": "cd frontend_user && npm run build",
    "lint:frontend_user": "cd frontend_user && npm run lint",

    "clean": "rm -rf backend/.venv MiniFish/backend/.venv InfoFish/backend/.venv frontend_user/.next frontend_user/node_modules"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=18"
  }
}
